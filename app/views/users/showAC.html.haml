%h1.title Upload File Json

%table#table
 %thead
  %tr
   %th User_id
   %th Timestamps
   %th Count
 %tbody
  - @activity_counts.each do |ac|
   %tr
    %td= ac.user_id
    %td= ac.time
    %td= ac.count

%p= show_flash

%p= link_to 'Back to users list', users_path


